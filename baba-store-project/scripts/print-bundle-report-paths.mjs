console.log(`
Bundle analyzer output (after successful build):
  .next/analyze/client.html   — browser JavaScript
  .next/analyze/nodejs.html   — Node.js server bundles
  .next/analyze/edge.html     — Edge runtime
`);
